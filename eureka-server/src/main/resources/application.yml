server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: host.docker.internal
  client:
    register-with-eureka: false
    fetch-registry: false